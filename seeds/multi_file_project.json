[
  "Scaffold a small Express REST API with a routes file, a controller file, and a models file, then wire them together.",
  "Add a new field to a database model and propagate the change through the migration file, the ORM model, and the API serializer.",
  "Extract a duplicated validation function out of two separate route handler files into a shared utils module and update both call sites.",
  "Add a new React component plus its stylesheet and its test file, then wire it into the parent page component.",
  "Rename a widely-used config key across a config file, the code that reads it, and the README that documents it.",
  "Add a new CLI subcommand: a new command file, register it in the CLI entrypoint, and add a usage example to the docs.",
  "Introduce a caching layer: add a cache module, wire it into an existing data-fetching function, and add a cache-invalidation call at the write site.",
  "Split a large single-file Flask app into blueprints across multiple files without changing any route behavior.",
  "Add authentication middleware, apply it to three existing route files, and update the test fixtures that hit those routes.",
  "Add a new environment-specific config file, update the config loader to merge it, and update the Docker Compose file to pass the new env var.",
  "Fix a bug where two files define slightly different copies of the same constant; consolidate to one shared source and update both imports.",
  "Add structured logging: create a logger module, replace print statements in two other files with calls to it.",
  "Add a database index migration and update the query file that would benefit from it, verifying the query plan improves.",
  "Introduce a shared TypeScript types file and update two components that were each locally redefining the same interface.",
  "Add a background job: a new job file, register it with the scheduler config, and add a manual trigger endpoint in the API file.",
  "Refactor a monolithic utils.py into two focused modules (io_utils.py, string_utils.py) and update every import across the codebase.",
  "Add rate limiting to an API: a middleware file, apply it in the server setup file, and add a config file entry for the limit.",
  "Wire a new third-party payment SDK into the codebase: an adapter module, a webhook handler file, and an update to the checkout route.",
  "Add end-to-end test coverage for a multi-step signup flow spanning the frontend form component and the backend route it posts to.",
  "Move hardcoded secrets out of three source files into a single .env-backed config module.",
  "Add pagination support to a list endpoint: update the query file, the serializer, and the frontend component consuming it.",
  "Introduce a feature flag system: add a flags module, gate one existing feature behind it in two different files.",
  "Add a health-check endpoint file and wire it into the app's route registration and the Docker health-check config.",
  "Consolidate two near-duplicate API client files (one for v1, one for v2) into a single versioned client module.",
  "Add input sanitization to a form handler and its corresponding backend validator, keeping both in sync."
]
